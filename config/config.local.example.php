<?php
/**
 * Example local configuration for XAMPP development.
 *
 * Copy this file to config/config.local.php (that filename is git-ignored)
 * and adjust the values for your own machine. Never commit
 * config/config.local.php or real credentials to GitHub.
 *
 * Default XAMPP MySQL settings are host 127.0.0.1, port 3306, user root
 * with an empty password — replace these with your own values if you
 * changed them in phpMyAdmin.
 */
return [
    'host' => '127.0.0.1',
    'port' => '3306',
    'name' => 'hue_u_xchange',
    'user' => 'root',
    'pass' => '',
];
